/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "avatars",
      "xsgames.co",
      "files.edgestore.dev",
      "avatar.iran.liara.run",
    ],
  },
};

export default nextConfig;
